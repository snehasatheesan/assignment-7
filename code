1. Create a program to count vowels in a given string using string functions.

import java.util.Scanner;

public class assign {

	public static void main(String[] args) {
	
		        Scanner g=new Scanner(System.in);
		        System.out.println("Enter a string:");
		        String t=g.nextLine();
		        String vowel="aeiou";
		        int count=0;
		        for (int i = 0; i < t.length(); i++) {
		            if(vowel.contains(""+t.charAt(i)))
		                count++;
		        }
		        System.out.println(" vowels in the given string is: "+count);
		    }
		}
______________________________________________________________________________________________________________________________________________________________________________________________________

2. Write a program to reverse the characters of a string.


import java.util.Scanner;

public class assign {

	    public static void main(String[] args) {
	        Scanner t=new Scanner(System.in);
	        System.out.println("Enter a string:");
	        String s=t.nextLine();
	        StringBuilder as=new StringBuilder(s);
	        as.reverse();
	        System.out.println(" reversed string"+" "+as);
	    }
	}
___________________________________________________________________________________________________________________________________________________________________________________________________________
